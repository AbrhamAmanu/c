#include <stdio.h>
#include <stdlib.h>

    /*
      CH-230-A
      a1 p4.[c]
      Abrham Amanu Eshetu
      aeshetu@jacobs-university.de
    */

int main()
{
    int x=17, y=4;
        printf("x=%d\n",x);
        printf("y=%d\n",y);
   int sum=x+y;
        printf("sum=%d\n",sum);
    int multiply=x*y;
        printf("product=%d\n",multiply);
    int difference=x-y;
        printf("difference=%d\n",difference);
    float Division=(float)x/y;
        printf("division=%f\n",Division);
    int remainder=x%y;
        printf("remainder of division=%d\n", remainder);
         return 0;
}
